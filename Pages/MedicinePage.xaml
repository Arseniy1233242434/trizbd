<Page x:Class="Pharmacy.Pages.MedicinePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Pharmacy.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MedicinePage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="200"></ColumnDefinition>

        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="1">
            <Button Margin="7" Height="25" Click="Button_Click">
                Добавить
            </Button>
            <Button Margin="7" Height="25" Click="Button_Click_1">
                Удалить
            </Button>

        </StackPanel>
        <ListView 
                  MouseDoubleClick="ListView_MouseDoubleClick_1"
ItemsSource="{Binding service.Users}"

SelectedItem="{Binding current}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>


                        <TextBlock>
<Run Text="Наименование: "/>
<Run Text="{Binding Name}"/>

                        </TextBlock>
                        <TextBlock>
<Run Text="Категория: "/>
<Run Text="{Binding Categoty.Name}"/>

                        </TextBlock>
                        <TextBlock>
<Run Text="Форма выпуска: "/>
<Run Text="{Binding Type.Name}"/>

                        </TextBlock>
                        <TextBlock>
<Run Text="Производитель: "/>
<Run Text="{Binding Manufacturer.Name}"/>

                        </TextBlock>
                        <TextBlock>
<Run Text="Страна производителя: "/>
<Run Text="{Binding Manufacturer.Country}"/>

                        </TextBlock>
                        <TextBlock>
<Run Text="Нужен ли рецепт: "/>
<Run Text="{Binding Prescription}"/>

                        </TextBlock>
                        <TextBlock>
<Run Text="Срок хранения(мес): "/>
<Run Text="{Binding ExpirationPeriod}"/>

                        </TextBlock>
                        <TextBlock>
<Run Text="Дозировка: "/>
<Run Text="{Binding Dosage}"/>

                        </TextBlock>
                        <TextBlock>
<Run Text="Условия хранения: "/>
<Run Text="{Binding Condition.Name}"/>

                        </TextBlock>

                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
